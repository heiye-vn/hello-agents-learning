### 一、 专业技能栏（Skill Set）写法示例

在简历的“专业技能”部分，不要只写“了解 Agent”，而是体现出**底层原理 + 生产框架**的深度：

> - **大模型与 Agent 架构**：深入理解大语言模型（LLM） Agent 核心设计范式，熟悉 **ReAct**（Thought-Action-Observation）、**Plan-and-Solve** 及 **Self-Reflection** 自我修正机制；
> - **底层引擎与手撸能力**：具备**从零手写 Agent 引擎**能力，掌握纯 Python 实现 Function Calling 参数解析、结构化 Prompt 约束及工具注册表（Tool Registry）设计；
> - **Memory 与 RAG 增强**：掌握 Agent 短期与长期记忆机制，熟悉基于向量数据库（Chroma/FAISS）的 RAG 上下文增强、历史对话摘要压缩与滑动窗口策略；
> - **主流框架与工程化**：熟练掌握 **LangGraph / LangChain** 的状态管理（StateGraph）与节点调度，具备多智能体（Multi-Agent）路由分发与协同通信的工程落地能力。

---

### 二、 项目经历栏（Project Experience）包装示例

你可以结合 `code/section15` 的综合实战（如智能代码解释器/研报助手/客服 Agent），将项目包装为具备含金量的项目：

#### 📝 项目名称：**基于自研轻量 Agent 引擎的智能研发/研报助手系统**

- **项目描述**：针对复杂多步骤任务处理场景，基于 LLM 与 ReAct 范式搭建的一款高灵活性 Agent 系统。支持动态工具调用、RAG 记忆检索与复杂任务拆解。
- **核心职责与技术实现**：
  1. **底层 Agent 引擎设计**：放弃重型框架依赖，使用纯 Python 手写核心 **ReAct 调度引擎**，实现低延迟的 Thought-Action 循环与流式（Streaming）状态响应；
  2. **工具链与 Function Calling 增强**：设计标准 Tool 注册接口与 JSON Schema 解析器，实现沙箱代码执行、Web 搜索等工具的动态绑定与异常重试（Retry）机制；
  3. **长期记忆与上下文管理**：结合 **Chroma 向量数据库** 实现 Agent 的长期记忆检索（RAG），采用滑动窗口与大模型摘要结合策略，降低 40% 的 Token 消耗并解决长文本遗忘问题；
  4. **任务拆解与多智能体协作**：引入 Router 分发节点与 Sub-Agent 模式，实现复杂大任务向子任务的自动拆解与结果汇聚。

---

### 三、 面试防坑指南（面试官怎么问？你怎么答？）

当你把这些写上简历后，面试官通常会问以下几个深度问题，你可以这样回答：

#### ❓ 面试官问：“你为什么选择自己手写 Agent 引擎，而不是直接用 LangChain？”

>**💡 高分回答**： “LangChain 封装层次很深，在轻量场景或特定定制化业务中存在性能开销和 Debug 困难的问题。我通过手撸 ReAct 循环、Tool 解析与 Memory 机制，彻底搞懂了 Agent 的底层数据流转。在实际生产中，我会根据业务复杂度进行选择——简单低延迟场景使用自研/轻量引擎，复杂图状态流转则使用 LangGraph。”

#### ❓ 面试官问：“Agent 在调用 Tool 时，如果 LLM 返回的 JSON 格式不对/报错了，你怎么处理？”

>**💡 高分回答**： “在手写引擎时我专门设计了**容错与自我修正机制（Self-Correction）**。当工具解析失败时，系统不会直接 Crash，而是把报错信息（Error Traceback）作为 Observation 重新喂给 LLM，触发 LLM 的自我修复重新生成正确的参数，最多重试 3 次；同时在 Prompt 层增加 JSON Schema 严格校验。”

#### ❓ 面试官问："请介绍一下 ReAct 范式的原理？"

>**💡 高分回答**： "ReAct 的全称是 Reasoning + Acting，核心是 **Thought-Action-Observation** 循环。模型先进行内部推理（Thought），决定需要做什么；然后执行一个外部动作（Action），比如调用搜索工具或执行代码；最后观察工具返回的结果（Observation），把结果加入上下文继续推理。这个循环反复进行，直到模型认为可以给出最终答案。
>
> 它解决了两个关键问题：纯推理（CoT）容易产生幻觉，因为不能获取实时信息；纯行动又缺乏推理判断，不知道该调什么工具。ReAct 把推理和行动交替进行，兼顾了准确性和灵活性。目前 LangChain Agent、LangGraph 的底层默认就是 ReAct 模式。"

#### ❓ 面试官问："Plan-and-Solve 和 ReAct 有什么区别？什么时候该用哪个？"

>**💡 高分回答**： "两者的核心区别在于**规划的时机**：
>
> - **ReAct** 是「边想边做」——每一步都是即时推理，适合**信息不确定、需要动态调整**的场景，比如搜索问答、实时信息查询。但缺点是容易「走偏」，因为没有全局视角。
> - **Plan-and-Solve** 是「先想后做」——先生成一个完整的多步计划，再按计划逐步执行。适合**步骤明确、可预见**的复杂任务，比如数据分析流水线、多步报告生成。
>
> 在实际生产中，更常见的是它的进化版 **Plan-and-Execute**，也就是 LangGraph 中的实现，加入了**动态重规划**能力——执行过程中如果发现计划有问题，可以根据中间结果调整后续步骤。我自己在做复杂多步任务时会优先选择 Plan-and-Execute，简单查询类任务用 ReAct。"

#### ❓ 面试官问："Reflexion（自我反思）机制是怎么工作的？有什么实际应用？"

>**💡 高分回答**： "Reflexion 的核心是一个**生成→评估→反思→重做**的循环，包含三个角色：
>
> 1. **Actor（行动者）**：根据任务生成答案或代码
> 2. **Evaluator（评估者）**：判断输出质量，比如跑单元测试、打分
> 3. **Self-Reflection（反思者）**：分析失败原因，生成一段反思文本
>
> 关键在于反思结果会被保存到**长期记忆**中，下一轮生成时会带上历史反思，避免重复犯错。这和人类「吃一堑长一智」是一个道理。
>
> 实际应用最典型的就是**代码生成**场景：Agent 写完代码后跑测试，如果失败就反思哪里写错了，然后修改代码再跑，直到所有测试通过。在我的项目中，Tool 调用失败后的 Self-Correction 机制本质上就是 Reflexion 思想的简化应用——把错误信息作为 Observation 反馈给 LLM，让它修正参数重新调用。"

#### ❓ 面试官问："你能对比一下主流 Agent 范式各自的优缺点和适用场景吗？"

>**💡 高分回答**：
>
> | 范式 | 核心思想 | 优点 | 缺点 | 适用场景 |
> |------|---------|------|------|---------|
> | **CoT** | 一步步推理 | 简单、零成本 | 不能获取外部信息 | 数学、逻辑推理 |
> | **ReAct** | 边推理边行动 | 灵活、通用性强 | 缺乏全局规划，复杂任务易走偏 | 搜索问答、工具调用 |
> | **Plan-and-Solve** | 先规划后执行 | 结构清晰、可控性强 | 初始计划可能不够灵活 | 多步骤流程任务 |
> | **Reflexion** | 自我反思改进 | 输出质量高、能从失败中学习 | 多轮调用成本高 | 代码生成、高质量写作 |
> | **LATS** | 树搜索择优 | 能找到最优解 | 计算成本极高 | 数学证明、竞赛编程 |
> | **Multi-Agent** | 多角色分工 | 可处理超复杂任务 | 架构复杂、协调成本高 | 大型工程、端到端系统 |
>
> "在实际生产中，这些范式经常**混合使用**。比如一个 Multi-Agent 系统中，整体用 Plan-and-Execute 做任务编排，每个子 Agent 内部用 ReAct 做工具调用，关键输出节点用 Reflexion 做质量把控。选型的核心原则是**任务复杂度匹配**——杀鸡不用牛刀，简单任务用 ReAct，复杂任务逐步升级。"

#### ❓ 面试官问："Multi-Agent 多智能体协作是怎么实现的？"

>**💡 高分回答**： "Multi-Agent 的核心是**角色分工 + 通信协调**。把一个复杂任务拆分给多个专业化的 Agent，每个 Agent 有自己的 System Prompt（角色定义）、Tool（工具权限）和 Memory（记忆空间）。
>
> 常见的协作模式有三种：
> 1. **顺序流水线**：Agent A 的输出作为 Agent B 的输入，适合有固定流程的任务（如：调研→写作→审校）
> 2. **路由分发**：一个 Router Agent 根据任务类型分发给不同的专业 Agent（如：客服场景按问题类型路由到售前/售后/技术 Agent）
> 3. **辩论/协商**：多个 Agent 对同一问题各自给出方案，通过多轮讨论达成共识（如：代码审查场景）
>
> 在框架层面，LangGraph 用 StateGraph 管理状态流转和节点调度，CrewAI 通过 Role-Task-Crew 三层抽象来定义团队协作，AutoGen 则更侧重于 Agent 间的多轮对话。我个人倾向于用 LangGraph，因为它的状态管理最灵活，可以实现条件路由和循环。"

#### ❓ 面试官问："你了解大模型的训练过程吗？SFT 和 RLHF 是什么？"

>**💡 高分回答**： "大模型训练分三个阶段：
>
> 1. **预训练（Pre-Training）**：在海量文本（万亿 Token 级别）上做 Next Token Prediction，让模型学会语言和世界知识。这一步成本极高，只有大公司能做。
> 2. **SFT（有监督微调）**：用人工标注的「指令→回答」数据集对预训练模型进行微调，让模型从'会续写'变成'会回答问题、听指令办事'。数据量通常在 1 万到 10 万条。
> 3. **RLHF / DPO（人类偏好对齐）**：通过人类对多个回答的偏好排序来进一步优化模型，让回答更有用、更安全、更诚实。RLHF 需要先训练一个奖励模型再用 PPO 强化学习优化；DPO 是更简单的替代方案，直接用偏好数据优化，跳过奖励模型。
>
> 作为 Agent 应用开发者，我不需要从零预训练模型，但了解 **LoRA/QLoRA 微调**是必要的。LoRA 只训练 0.1%~1% 的低秩适配参数，一张 RTX 4090 就能微调 7B~70B 的模型。在实际项目中，如果通用模型在特定领域（比如法律、医疗）表现不够好，我会用领域数据做 LoRA 微调来提升效果。"

---

### 💡 总结建议

1. **先手撸完核心代码**：把 `section03`（ReAct）、`section04`（Tool Call）、`section07`（RAG）这几章的代码打通、理解透。
2. **做一个属于你自己的 Agent 小应用**：基于所学知识，改造成一个你自己感兴趣的工具（比如：自动分析 Git Commit 的 Agent、命令行天气与新闻 Agent）。
3. **自信地写上简历**：你不仅懂框架，更懂底层原理，这在当前市场上是非常抢手的 Agent 研发人才！
