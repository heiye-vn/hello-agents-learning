### 1. 什么是上下文工程？

**上下文工程**是 AI Agent 开发中的一门核心学科，指的是**为 LLM 设计和构建恰到好处的输入信息（上下文）的系统性方法**。

它的核心目标很简单：**让模型在每一次调用时，都能看到完成任务所需的全部信息，且不被无关信息干扰。**

> [!NOTE]
>
> LLM 的能力上限在很大程度上取决于你"喂"给它的上下文质量。同一个模型，给不同的上下文，产出可以天差地别。Prompt Engineering 关注的是"怎么问"，而 Context Engineering 关注的是"把什么信息放进去"。

**上下文的来源：**

| 来源                     | 说明                                   | 示例                                      |
| :----------------------- | :------------------------------------- | :---------------------------------------- |
| **System Instructions**  | 系统指令，定义角色和行为规范           | `"你是一位资深的Python数据工程顾问"`      |
| **Conversation History** | 对话历史，提供多轮对话的连贯性         | 之前讨论过的技术栈选择                    |
| **Memory**               | 记忆系统，持久化的用户/项目知识        | `"用户正在开发数据分析工具，使用 Pandas"` |
| **RAG (Retrieval)**      | 检索增强，从外部知识库动态拉取相关信息 | 根据用户问题检索技术文档                  |

> [!WARNING]
>
> **System prompt 是上下文的主要载体之一，但不是唯一载体。** 好的上下文工程会把不同类型的信息放到最适合的 role 中。